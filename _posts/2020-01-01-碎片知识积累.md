---
layout: post
title: "碎片知识"
subtitle: '碎片知识积累，杂'
author: "WangChiech"
header-style: text
tags:
  - 框架、库
---

1. git

   ```bash
   # --depth=1 表示仅下载最后一次的版本历史
   $ git clone -b gh-page https://github.com/... --depth=1
   ```

2. http-server 开启本地服务

   ```bash
   # 安装
   $ npm install --global http-server
   
   # 在文档目录下执行
   $ hs -o
   # 开启一个HTTP服务，并自动打开浏览器访问 http://localhost:8080/地址
   ```

   