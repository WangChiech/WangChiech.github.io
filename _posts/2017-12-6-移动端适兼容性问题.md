---
layout: post
title: "移动端、pc端兼容性问题"
subtitle: '常见移动端、pc端兼容性问题'
author: "WangChiech"
header-style: text
tags:
  - 移动端
---

> 对于常见移动端、pc端兼容性问题的梳理

## 一、移动端

1. 安卓浏览器看背景图片，有些设备会模糊。

   想让图片在手机里显示更为清晰，必须使用2x的背景图来代替img标签（一般情况都是用2倍）。例如一个div的宽高是100x100，背景图必须得200x200，然后background-size:contain;，这样显示出来的图片就比较清晰了。

2. 假如手机网站不用兼容IE浏览器

   一般使用zeptojs。zeptojs内置Touch events方法，具体可以看http://zeptojs.com/#Touch events看了一下zeptio新版的API，已经支持IE10以上浏览器，对zeptojs可以选择使用！

3. 防止手机中网页放大和缩小

   使用viewport使页面禁止缩放。 通常把user-scalable设置为0来关闭用户对页面视图缩放的行为。

4. format-detection(format-detection 启动或禁用自动识别页面中的电话号码。)

   默认情况下，设备会自动识别任何可能是电话号码的字符串。设置telephone=no可以禁用这项功能。

5. 旋转屏幕时，字体大小调整的问题

   ```css
   html, body, form, fieldset, p, div, h1, h2, h3, h4, h5, h6{
   	-webkit-text-size-adjust:100%;
   }
   ```

6. 圆角bug，某些Android手机圆角失效

   ```css
   background-clip: padding-box; 
   ```

## 二、pc 端

1. 消除 IE10 里面的那个叉号

```css
input:-ms-clear{
  display:none;
} 
```

