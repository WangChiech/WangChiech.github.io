---
layout: post
title: "项目1"
subtitle: ''。'
author: "WangChiech"
header-style: text
tags:
  - 项目
---

### yarn.lock

yarn 是依赖管理工具。

**官方说明**

> 为了跨机器安装得到一致的结果，Yarn 需要比你配置在 package.json 中的依赖列表更多的信息。 Yarn 需要准确存储每个安装的依赖是哪个版本。
> 为了做到这样，Yarn 使用一个你项目根目录里的 yarn.lock 文件。这可以媲美其他像 Bundler 或 Cargo 这样的包管理器的 lockfiles。它类似于 npm 的 npm-shrinkwrap.json，然而他并不是有损的并且它能创建可重现的结果。
>
> 使用Yarn CLI添加/升级/删除 依赖项的时，它将自动更新到您的yarn.lock文件。不要直接编辑这个文件，因为很容易破坏某些东西。

```js
yarn init => // 生成package.json
yarn install => //生成yarn.lock
```

### package.json、package-lock.json

- package.json:主要用来定义项目中需要依赖的包
- package-lock.json：在 `npm install`时候生成一份文件，用以记录当前状态下实际安装的各个npm package的具体来源和版本号。
- '^' :放在版本号之前，表示向后兼容依赖，就是在大版本号不变的情况下，下载最新版的包
  项目中引入的包版本号之前经常会加^号，每次在执行npm install之后，下载的包都会发生变化，为了系统的稳定性考虑，***每次执行完npm install之后会对应生成package-lock文件***，该文件记录了上一次安装的具体的版本号，相当于是提供了一个参考，在出现版本兼容性问题的时候，就可以参考这个文件来修改版本号即可。

### .gitignore

- 忽略文件提交
  - /mtk 过滤整个文件夹
  - *.zip 过滤所有.zip文件
  - /mtk/do.c 过滤某个具体文件
  - !/mtk/one.txt 追踪（不过滤）某个具体文件

> 如果你创建.gitignore文件之前就push了某一文件，那么即使你在.gitignore文件中写入过滤该文件的规则，该规则也不会起作用，git仍然会对该文件进行版本管理。

```js
.DS_Store
node_modules
/dist

# local env files
.env.local
.env.*.local

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor directories and files
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
```

### postcss.config.js

- 给postcss用的
- postcss 是帮我们后处理css ，css已经编译完成了，在stylus-loader编译成css之后，在通过postcss优化css，通过一系列组件去优化，比如以下，通过autoprefixer 添加css前缀

### .babel.config.js

- babel JavaScript编译器，将ES6+转换成低版本

- .babel.config.js 为配置文件

  ```js
  module.exports = {
      plugins: {
          // Autoprefixer解析CSS文件并且添加浏览器前缀到CSS规则里，
          // 使用Can I Use的数据来决定哪些前缀是需要的。
          autoprefixer: {}
      }
  }
  ```

### .eslintrc.js

- ESLint是一个JavaScript代码静态检查工具，语法错误、潜在bug、维持一致的编码

- .eslintrc.js 为配置文件

  ```js
  /**
   * off | 0 : 表示不验证规则
   * warn | 1 : 表示验证规则，当不满足时，给警告
   * error | 2 : 表示验证规则，不满足时报错
   */
  module.exports = {
      root: true,
      env: {
          browser: true,
          es6: true
      },
      globals: {
          _: true,
          jt: true,
          oauth: true,
          req: true,
          api: true,
          CNST: true
      },
      'extends': [
          'plugin:vue/essential',
          '@vue/standard'
      ],
      rules: {
          'no-console': process.env.NODE_ENV === 'production' ? 'error' : 'off',
          'no-': process.env.NODE_ENV === 'production' ? 'error' : 'off',
          // 缩进4个空格
          'indent': [0, 4],
          // 不加分号
          'semi': [2, 'never'],
          'camelcase': 0
      },
      parserOptions: {
          parser: 'babel-eslint'
      }
  }
  ```

### .env 文件

> package.json中配置
>
> ```js
> "scripts": {
>         "start": "vue-cli-service serve",
>         "lint": "vue-cli-service lint",
>         // 想要修改模式下默认的环境变量,可以通过--mode来实现
>         "test": "vue-cli-service build --mode test",
>         "gray": "vue-cli-service build --mode gray",
>         "prod": "vue-cli-service build --mode production"
>     },
> ```

- .env.development 开发环境下的配置文件

  ```js
  VUE_APP_API_PREFIX = /api
  
  VUE_APP_PUBLIC_PATH = /
  
  VUE_APP_MY_APRV_URL = http://ceshi.com/ceshi/ceshi
  
  VUE_APP_OAUTH_ENABLE = true
  ```

- .env.test测试环境下的配置文件

  ```js
  NODE_ENV = 'production'
  
  VUE_APP_API_PREFIX = /api
  
  VUE_APP_PUBLIC_PATH = /
  
  VUE_APP_MY_APRV_URL = http://ceshi.com/ceshi/ceshi
  
  VUE_APP_APRV_SRCH_URL = http://ceshi.com/ceshi/ceshi
  
  VUE_APP_OAUTH_ENABLE = true
  ```

- .env.gray 灰度环境下的配置文件

  ```js
  NODE_ENV = 'production'
  
  VUE_APP_API_PREFIX = /api
  
  VUE_APP_PUBLIC_PATH = /
  
  VUE_APP_MY_APRV_URL = http://ceshi.com/ceshi/ceshi
  
  VUE_APP_APRV_SRCH_URL = http://ceshi.com/ceshi/ceshi
  ```

- .env.production 生产环境下的配置文件

  ```js
  VUE_APP_API_PREFIX = /api
  
  VUE_APP_PUBLIC_PATH = /
  
  VUE_APP_MY_APRV_URL = http://ceshi.com/ceshi/ceshi
  
  VUE_APP_APRV_SRCH_URL = http://ceshi.com/ceshi/ceshi
  
  VUE_APP_LOGIN_URL = http://ceshi.com/ceshi/ceshi?ReturnUrl=
  
  VUE_APP_LOGOUT_URL = http://ceshi.com/ceshi/logout?ReturnUrl=
  ```

  

